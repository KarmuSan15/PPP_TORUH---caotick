// Importación del componente Stack desde la librería "expo-router".
// Stack es un componente que maneja la navegación entre pantallas en una aplicación Expo.
import { Stack } from "expo-router";

// Definición del componente funcional RootLayout, que es la estructura base de la aplicación.
export default function RootLayout() {
  // Retorna un componente Stack, que gestionará las pantallas de la aplicación.
  return <Stack />;
}
